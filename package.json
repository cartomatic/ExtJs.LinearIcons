{
    "namespace": "Ext",

    "name": "linear-icons",
    "type": "code",

    "framework": "ext",
    "toolkit": "classic",
    "theme": "theme-neptune",
    //"requires": [
    //    ...
    //],

    "creator": "Sencha",
    "summary": "Package wrapper for Linear icons",
    "detailedDescription": "Sencha Cmd package wrapper for Linear Icons (https://linearicons.com/)",

    "version": "6.2.0.981",
    "compatVersion": "4.3.0",
    "format": "1",

    "output": "${framework.dir}/build/packages/${package.name}",
    "local": true,

    "resource": {
        "paths": ""
    },

    "resources": [
        {
            "path": "${package.dir}/resources",
            "output": "shared"
        }
    ],

    "sass": {
        "namespace": "Ext",

        "etc": "${package.dir}/sass/etc/all.scss",

        "var": "${package.dir}/sass/var",

        "src": [
            "${package.dir}/sass/src",
            "${package.dir}/sass/src/all.scss"
        ]
    },

    "classpath": "${package.dir}/src",

    "overrides": "${package.dir}/overrides",

    "slicer": null
}
